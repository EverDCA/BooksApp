<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>No permitido | BooksApp</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="min-h-screen flex flex-col bg-gradient-to-t from-zinc-900 via-gray-900 to-slate-900 text-white">
    <%- include('books/navbar.ejs', { user: user }) %>
    <div class="flex-1 flex flex-col items-center justify-center animate-fade">
      <dialog id="modal-forbidden" class="modal modal-open">
        <div class="modal-box max-w-md text-center">
          <h2 class="text-2xl font-bold text-error mb-4">Acceso no permitido</h2>
          <p class="mb-4">No tienes permisos para acceder a esta sección.</p>
          <a href="/dashboard" class="btn btn-primary">Ir al inicio</a>
        </div>
      </dialog>
    </div>
    <%- include('footer.ejs') %>
  </body>
</html>
