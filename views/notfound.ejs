<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Página no encontrada | BooksApp</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="min-h-screen flex flex-col bg-gradient-to-t from-zinc-900 via-gray-900 to-slate-900 text-white">
    <%- include('books/navbar.ejs', { user: typeof user !== 'undefined' ? user : null }) %>
    <div class="flex-1 flex flex-col items-center justify-center animate-fade">
      <div class="card bg-base-200 border border-base-300 shadow-lg max-w-md w-full p-8 text-center">
        <h1 class="text-5xl font-bold text-error mb-4">404</h1>
        <h2 class="text-2xl font-bold mb-2">Página no encontrada</h2>
        <p class="mb-6">La ruta que intentas visitar no existe o ha sido movida.</p>
        <a href="/dashboard" class="btn btn-primary">Ir al inicio</a>
      </div>
    </div>
    <%- include('footer.ejs') %>
  </body>
</html>
